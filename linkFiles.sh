#!/bin/zsh

# Symlink dotfiles to the repo
ln -fs ~/repositories/dotfiles/.macos ~/.macos
ln -fs ~/repositories/dotfiles/.vimrc ~/.vimrc
ln -fs ~/repositories/dotfiles/.zprofile ~/.zprofile
ln -fs ~/repositories/dotfiles/.zshrc ~/.zshrc
